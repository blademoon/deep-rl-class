# # Обучение первого агента глубокого обучения с подкреплением 🤖 [[hands-on]]

      <CourseFloatingBanner classNames="absolute z-10 right-0 top-0"
      notebooks={[
        {label: "Google Colab", value: "https://colab.research.google.com/github/huggingface/deep-rl-class/blob/master/notebooks/unit1/unit1.ipynb"}
        ]}
        askForHelpUrl="http://hf.co/join/discord" />

Теперь, когда вы изучили основы Reinforcement Learning, вы готовы обучить своего первого агента и поделиться им с сообществом через Hub 🔥:
Агент Lunar Lander, который научится правильно садиться на Луну 🌕.

<img src="https://huggingface.co/datasets/huggingface-deep-rl-course/course-images/resolve/main/en/unit1/lunarLander.gif" alt="LunarLander">

И, наконец, вы **загрузите обученного агента в Hub Hugging Face 🤗, бесплатную, открытую платформу, где люди могут делиться ML-моделями, наборами данных и демо.**

Благодаря нашей <a href="https://huggingface.co/spaces/huggingface-projects/Deep-Reinforcement-Learning-Leaderboard">таблице лидеров</a> вы сможете сравнить свои результаты с результатами других однокурсников и обменяться передовым опытом, чтобы улучшить результаты своего агента. Кто победит в испытании Блока 1 🏆?

Чтобы подтвердить эту практическую работу для [процесса сертификации](https://huggingface.co/deep-rl-course/en/unit0/introduction#certification-process)Вам необходимо отправить обученную модель в Hub и **получить результат >= 200.**

Чтобы узнать свой результат, перейдите в [таблицу результатов](https://huggingface.co/spaces/huggingface-projects/Deep-Reinforcement-Learning-Leaderboard) и найти свою модель, **the result = mean_reward - std от reward**.

**Если вы не нашли свою модель, перейдите в нижнюю часть страницы и нажмите на кнопку "refresh".**

Более подробную информацию о процессе сертификации можно найти в этом разделе 👉 https://huggingface.co/deep-rl-course/en/unit0/introduction#certification-process

А проверить свой прогресс можно здесь 👉 https://huggingface.co/spaces/ThomasSimonini/Check-my-progress-Deep-RL-Course

Итак, начнем! 🚀

**Для запуска практической работы нажмите на кнопку Open In Colab**. 👇 :

[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/huggingface/deep-rl-class/blob/master/notebooks/unit1/unit1.ipynb)

Мы настоятельно **рекомендуем студентам использовать Google Colab для практических занятий**, а не выполнять их на своих персональных компьютерах. 

Используя Google Colab, **вы можете сосредоточиться на обучении и экспериментах, не заботясь о технических аспектах** настройки среды.

# Блок 1: Обучение первого агента глубокого обучения с подкреплением 🤖

<img src="https://huggingface.co/datasets/huggingface-deep-rl-course/course-images/resolve/main/en/unit1/thumbnail.jpg" alt="Unit 1 thumbnail" width="100%">

В этом блокноте вы обучите своего **первого агента Deep Reinforcement Learning** - агента Lunar Lander, который будет учиться **правильно приземляться на Луну 🌕**. Используя [Stable-Baselines3](https://stable-baselines3.readthedocs.io/en/master/) библиотеку Deep Reinforcement Learning, делиться им с сообществом и экспериментировать с различными конфигурациями


### Среда 🎮

- [LunarLander-v2](https://gymnasium.farama.org/environments/box2d/lunar_lander/)

### Используемые библиотеки 📚

- [Stable-Baselines3](https://stable-baselines3.readthedocs.io/en/master/)

Мы постоянно стараемся улучшить наши руководства, поэтому **если вы обнаружите какие-то проблемы в этом блокноте**, пожалуйста, [откройте issue на Github Repo](https://github.com/huggingface/deep-rl-class/issues).

## Цели данного блокнота 🏆

В конце блокнота вы найдете:

- Возможность использования библиотеки среды **Gymnasium**.
- Возможность использования **Stable-Baselines3**, библиотеки глубокого обучения с подкреплением.
- Сможете **разместить своего обученного агента в Hub** с красивым видеоповтором и результатами оценки 🔥.


## Этот блокнот из курса "Глубокое обучение с подкреплением"

<img src="https://huggingface.co/datasets/huggingface-deep-rl-course/course-images/resolve/main/en/notebooks/deep-rl-course-illustration.jpg" alt="Иллюстрация к курсу Deep RL"/>

В этом бесплатном курсе вы узнаете:

- 📖 Изучите глубокое обучение с подкреплением в **теории и на практике**
- 🧑‍💻 Научитесь **использовать известные библиотеки Deep RL**, такие как Stable Baselines3, RL Baselines3 Zoo, CleanRL и Sample Factory 2.0
- 🤖 Обучите **агентов в уникальной среде**
- 🎓 **Выполнив 80% заданий, сможете получить сертификат об окончании курса**.

И многое другое!

Проверьте 📚 учебный план 👉 https://simoninithomas.github.io/deep-rl-course

Не забудьте **<a href="http://eepurl.com/ic5ZUD">подписаться на курс</a>** (мы собираем вашу электронную почту, чтобы иметь возможность **высылать вам ссылки, когда каждый модуль будет опубликован, и информировать вас о задачах и обновлениях).**

Лучший способ поддерживать связь и задавать вопросы - **присоединиться к нашему discord-серверу** для обмена информацией с сообществом и с нами 👉🏻 https://discord.gg/ydHrjt3WP5

## Предварительные условия 🏗️

Прежде чем погрузиться в блокнот, необходимо:

🔲 📝 **[Прочитайте Блок 0](https://huggingface.co/deep-rl-course/unit0/introduction)** который предоставит вам всю **информацию о курсе и поможет включиться в работу.** 🤗

🔲 📚 **Развить понимание основ обучения с подкреплением** (MC, TD, Гипотеза о вознаграждениях...) [прочитав Блок 1](https://huggingface.co/deep-rl-course/unit1/introduction).

## Небольшой обзор глубокого обучения с подкреплением 📚

<img src="https://huggingface.co/datasets/huggingface-deep-rl-course/course-images/resolve/main/en/unit1/RL_process_game.jpg" alt="The RL process" width="100%">

Давайте сделаем небольшой обзор того, что мы узнали в первом блоке:

- Обучение с подкреплением - это **вычислительный подход к обучению на основе действий**. Мы создаем агента, который учится у среды, **взаимодействуя с ней методом проб и ошибок** и получая вознаграждение (отрицательное или положительное) в качестве обратной связи.

- Целью любого агента RL является **максимизация ожидаемого кумулятивного вознаграждения** (также называемого ожидаемой доходностью), поскольку RL основана на _гипотезе вознаграждения_, согласно которой все цели могут быть описаны как максимизация ожидаемого кумулятивного вознаграждения.

- Процесс RL представляет собой **цикл, который выводит последовательность состояний, действий, вознаграждений и следующего состояния.**

- Чтобы рассчитать ожидаемое кумулятивное вознаграждение (ожидаемую доходность), **мы дисконтируем вознаграждения**: вознаграждения, которые наступают раньше (в начале игры), более вероятны, так как они более предсказуемы, чем долгосрочное будущее вознаграждение.

- Для решения RL-задачи необходимо **найти оптимальную политику**; политика - это "мозг" вашего ИИ, который говорит нам, какие действия следует предпринять в том или ином состоянии. Оптимальной является та, которая дает действия, обеспечивающие максимальную ожидаемую доходность.

Существует **два** способа найти оптимальную политику:

- Путем **обучения политики напрямую**: методы, основанные на политике.
- Путем **обучения функции ценности**, которая говорит нам об ожидаемой доходности агента в каждом состоянии, и использования этой функции для определения нашей политики: методы, основанные на ценности.
- Наконец, мы говорили о глубоком RL, потому что **мы вводим глубокие нейронные сети для оценки действий (policy-based) или для оценки ценности состояния (value-based), отсюда и название "глубокое".**

# Давайте обучим нашего первого агента глубокого обучения с подкреплением и загрузим его в Hub 🚀

## Получить сертификат 🎓

Для подтверждения этой практической работы для [процесса сертификации](https://huggingface.co/deep-rl-course/en/unit0/introduction#certification-process), Вам необходимо передать обученную модель в Hub и **получить результат >= 200.**

Чтобы узнать свой результат, перейдите в [таблицу результатов](https://huggingface.co/spaces/huggingface-projects/Deep-Reinforcement-Learning-Leaderboard)  и найти свою модель, **the result = mean_reward - std от reward**.

Более подробную информацию о процессе сертификации можно найти в этом разделе 👉 https://huggingface.co/deep-rl-course/en/unit0/introduction#certification-process

## Настройка GPU 💪

- Чтобы **ускорить обучение агента, мы будем использовать GPU**. Для этого перейдите в меню `Runtime > Change Runtime type`.

<img src="https://huggingface.co/datasets/huggingface-deep-rl-course/course-images/resolve/main/en/notebooks/gpu-step1.jpg" alt="GPU Step 1">

- `Hardware Accelerator > GPU`

<img src="https://huggingface.co/datasets/huggingface-deep-rl-course/course-images/resolve/main/en/notebooks/gpu-step2.jpg" alt="GPU Step 2">

## Установка зависимостей и создание виртуального экрана 🔽

Первым шагом является установка зависимостей, мы установим несколько из них.

- `gymnasium[box2d]`: Содержит среду LunarLander-v2 🌛
- `stable-baselines3[extra]`: Библиотека глубокого обучения с подкреплением.
- `huggingface_sb3`: Дополнительный код для Stable-baselines3 для загрузки и выгрузки моделей из Hugging Face 🤗 Hub.

Чтобы упростить работу, мы создали скрипт для установки всех этих зависимостей.

```bash
apt install swig cmake
```

```bash
pip install -r https://raw.githubusercontent.com/huggingface/deep-rl-class/main/notebooks/unit1/requirements-unit1.txt
```

В процессе работы с ноутбуком нам нужно будет сгенерировать видеозапись повтора. Для того, чтобы сделать это с помощью colab, **нам необходимо иметь виртуальный экран для визуализации среды** (и, соответственно, записи кадров).

Таким образом, следующая ячейка установит библиотеки виртуального экрана, создаст и запустит виртуальный экран 🖥

```bash
sudo apt-get update
apt install python-opengl
apt install ffmpeg
apt install xvfb
pip3 install pyvirtualdisplay
```

Чтобы убедиться в том, что установленные новые библиотеки используются, **иногда требуется перезапустить среду выполнения блокнота**. Следующая ячейка приведет к сбою **во время выполнения (runtime), поэтому нужно будет подключиться заново и запустить код, начиная с этого места**. Благодаря этому трюку **мы сможем запустить наш виртуальный экран.**

```python
import os

os.kill(os.getpid(), 9)
```

```python
# Виртуальный дисплей
from pyvirtualdisplay import Display

virtual_display = Display(visible=0, size=(1400, 900))
virtual_display.start()
```

## Импорт пакетов 📦

Еще одна библиотека, которую мы импортируем, - huggingface_hub **для возможности загрузки и скачивания обученных моделей с хаба.**


Hugging Face Hub 🤗 работает как центральное место, где каждый может обмениваться и исследовать модели и наборы данных. В нем есть версионирование, метрики, визуализации и другие функции, которые позволят вам легко сотрудничать с другими.

Здесь можно ознакомиться со всеми доступными моделями глубокого обучения с подкреплением👉 https://huggingface.co/models?pipeline_tag=reinforcement-learning&sort=downloads



```python
import gymnasium

from huggingface_sb3 import load_from_hub, package_to_hub
from huggingface_hub import (
    notebook_login,
)  # Войти в нашу учетную запись Hugging Face, чтобы иметь возможность загружать модели в Hub.

from stable_baselines3 import PPO
from stable_baselines3.common.env_util import make_vec_env
from stable_baselines3.common.evaluation import evaluate_policy
from stable_baselines3.common.monitor import Monitor
```

## Понимание Gymnasium и принципов его работы 🤖

🏋 Библиотека, содержащая нашу среду, называется Gymnasium.
**Вы будете часто использовать Gymnasium в Deep Reinforcement Learning.**

Gymnasium - это **новая версия библиотеки Gym** [поддерживается фондом Farama Foundation](https://farama.org/).

Библиотека Gymnasium предоставляет две возможности:

- Интерфейс, позволяющий **создавать среды RL.**
- **Коллекцию сред** (gym-control, atari, box2D...).

Рассмотрим пример, но сначала вспомним цикл RL.

<img src="https://huggingface.co/datasets/huggingface-deep-rl-course/course-images/resolve/main/en/unit1/RL_process_game.jpg" alt="The RL process" width="100%">

На каждом шаге:
- Наш агент получает **состояние (S0)** от **среды** - мы получаем первый кадр нашей игры (среды).
- На основании этого **состояния (S0),** агент совершает **действие (A0)** - наш агент перемещается вправо.
- Среда переходит в **новое** **состояние (S1)** - новый кадр.
- Среда дает некоторое **вознаграждение (R1)** агенту - мы не умерли *(Positive Reward +1)*.


С Gymnasium:

1️⃣ Мы создаем нашу среду с помощью `gymnasium.make()`.

2️⃣ Мы возвращаем среду в исходное состояние командой `observation = env.reset()`.

На каждом шаге:

3️⃣ Получаем действие, используя нашу модель (в нашем примере мы берем случайное действие)

4️⃣ Используя `env.step(action)`, мы выполняем это действие в среде и получаем
- `observation` (наблюдение) : Новое состояние (st+1)
- `reward` (вознаграждение): Вознаграждение, которое мы получим после выполнения действия
- `terminated`: Указывает, завершился ли эпизод (агент достиг терминального состояния)
- `truncated`: Введен в новой версии, указывает на ограничение по времени или, например, на выход агента за пределы среды.
- `info`: Словарь, предоставляющий дополнительную информацию (зависит от среды).

Более подробные пояснения можно найти здесь 👉 https://gymnasium.farama.org/api/env/#gymnasium.Env.step

Если эпизод прерывается (terminated):
- Мы возвращаем среду в исходное состояние командой `observation = env.reset()`

**Давайте рассмотрим пример! ** Обязательно прочитайте код


```python
import gymnasium as gym

# Сначала мы создадим среду под названием LunarLander-v2
env = gym.make("LunarLander-v2")

# Затем мы сбросим эту среду
observation, info = env.reset()

for _ in range(20):
    # Выполним случайное действие
    action = env.action_space.sample()
    print("Action taken:", action)

    # Выполним это действие в среде и получим
    # next_state, reward, terminated, truncated and info
    observation, reward, terminated, truncated, info = env.step(action)

    # Если игра прерывается (в нашем случае мы приземлились, разбились) или обрывается (таймаут)
    if terminated or truncated:
        # Reset the environment
        print("Environment is reset")
        observation, info = env.reset()

env.close()
```

## Создание среды LunarLander 🌛 и понимание принципов ее работы

### Среда 🎮

В этом первом уроке мы будем обучать нашего агента, [Lunar Lander](https://gymnasium.farama.org/environments/box2d/lunar_lander/), **правильно приземляться на Луну**. Для этого агенту необходимо научиться **адаптировать свою скорость и положение (горизонтальное, вертикальное и угловое) для правильного приземления**.

---


💡 Хорошей привычкой при начале использования среды является проверка ее документации

👉 https://gymnasium.farama.org/environments/box2d/lunar_lander/

---


Посмотрим, как выглядит среда:


```python
# Мы создаем нашу среду с помощью gym.make("<name_of_the_environment>")
env = gym.make("LunarLander-v2")
env.reset()
print("_____OBSERVATION SPACE_____ \n")
print("Observation Space Shape", env.observation_space.shape)
print("Sample observation", env.observation_space.sample())  # Получим случайное наблюдение
```

С помощью `Observation Space Shape (8,)` мы видим, что наблюдение представляет собой вектор размера 8, в котором каждое значение содержит различную информацию о посадочном устройстве:
- Горизонтальная координата площадки (x)
- Вертикальная координата площадки (y)
- Горизонтальная скорость (x)
- Вертикальная скорость (y)
- Угол
- Угловая скорость
- Коснулась ли точка контакта левой ноги земли (boolean)
- Коснулась ли точка контакта правой ноги земли (boolean)


```python
print("\n _____ACTION SPACE_____ \n")
print("Action Space Shape", env.action_space.n)
print("Action Space Sample", env.action_space.sample())  # Выполняем случайное действие
```

Пространство действий (множество возможных действий агента) дискретно и имеет 4 варианта 🎮: 

- Действие 0: Ничего не делать,
- Действие 1: Включение двигателя левой ориентации,
- Действие 2: Включение основного двигателя,
- Действие 3: Включение двигателя правой ориентации,

Функция вознаграждения (функция, которая будет выдавать вознаграждение на каждом временном интервале (шаге) ) 💰:

После каждого шага выдается награда. Общая награда за эпизод равна **сумме наград за все шаги в рамках этого эпизода**.

За каждый шаг - вознаграждение:

- Увеличивается/уменьшается тем больше, чем ближе/дальше находится посадочный модуль к посадочной площадке.
- Увеличивается/уменьшается в зависимости от того, насколько медленнее/быстрее движется шасси.
- Уменьшается тем больше, чем сильнее наклонена посадочная платформа (угол не горизонтальный).
- Увеличивается на 10 очков за каждую ногу, соприкасающуюся с землей.
- Уменьшается на 0,03 пункта за каждый кадр, в котором работает боковой двигатель.
- уменьшается на 0,3 пункта за каждый кадр работы главного двигателя.

Эпизод получает **дополнительное вознаграждение в размере -100 или +100 очков за то, что разбился или благополучно приземлился соответственно.**

Эпизод **считается решенным, если он набрал не менее 200 баллов*.

#### Векторизованная среда

- Мы создаем векторизованную среду (метод объединения нескольких независимых сред в одну среду) из 16 сред, таким образом, **мы получим более разнообразный опыт во время обучения**.

```python
# Создание среды
env = make_vec_env("LunarLander-v2", n_envs=16)
```

## Создание модели 🤖

- Мы изучили нашу среду и поняли задачу: **уметь правильно посадить Lunar Lander на посадочную площадку, управляя левым, правым и главным двигателем ориентации**. Теперь давайте построим алгоритм, с помощью которого мы будем решать эту задачу 🚀.

- Для этого мы воспользуемся нашей первой библиотекой Deep RL, [Stable Baselines3 (SB3)](https://stable-baselines3.readthedocs.io/en/master/).

- SB3 - это набор **надежных реализаций алгоритмов обучения с подкреплением в PyTorch**.

---

💡 Хорошей привычкой при использовании новой библиотеки является погружение в первую очередь в документацию: https://stable-baselines3.readthedocs.io/en/master/ and then try some tutorials.

----

<img src="https://huggingface.co/datasets/huggingface-deep-rl-course/course-images/resolve/main/en/unit1/sb3.png" alt="Stable Baselines3">

Для решения этой задачи мы будем использовать SB3 **PPO**. [PPO (он же Proximal Policy Optimization - Проксимальная оптимизация политики) - один из алгоритмов SOTA (state of the art) глубокого обучения с подкреплением, который вы будете изучать в рамках данного курса].(https://stable-baselines3.readthedocs.io/en/master/modules/ppo.html#example%5D).

PPO - это сочетание:
- *Метод обучения с подкреплением на основе ценности действий*: обучение функции "действие-ценность", которая будет определять **наиболее ценное действие, которое следует предпринять при заданных состоянии и действии**.
- *Метод обучения с подкреплением на основе политики *: обучение политики, которая **даст нам распределение вероятностей по действиям**.

Stable-Baselines3 легко настраивается:

1️⃣ Вы **создаете свою среду** (в нашем случае это было сделано выше)

2️⃣ Вы определяете **модель, которую хотите использовать, и инстанцируете эту модель** `model = PPO("MlpPolicy")`

3️⃣ Вы **обучаете агента**, используя `model.learn` и определяя количество временных шагов обучения

```
# Создание среды
env = gym.make('LunarLander-v2')

# Инстанцирование агента
model = PPO('MlpPolicy', env, verbose=1)
# Train the agent
model.learn(total_timesteps=int(2e5))
```



```python
#  НЕОБХОДИМО СДЕЛАТЬ: Определить архитектуру PPO MlpPolicy
# Мы используем MultiLayerPerceptron (многослойный перцептрон, MLPPolicy), поскольку вход является вектором,
# Если бы в качестве входа у нас были фреймы, то мы бы использовали CnnPolicy
model =
```

#### Решение

```python
# РЕШЕНИЕ
# Мы добавили несколько параметров для ускорения обучения
model = PPO(
    policy="MlpPolicy",
    env=env,
    n_steps=1024,
    batch_size=64,
    n_epochs=4,
    gamma=0.999,
    gae_lambda=0.98,
    ent_coef=0.01,
    verbose=1,
)
```

## Обучение агента PPO 🏃
- Давайте обучим нашего агента на 1 000 000 временных шагов, не забудьте использовать GPU в Colab. Это займет примерно ~20 мин, но можно использовать меньшее количество временных шагов, если вы просто хотите попробовать.
- Во время тренинга сделайте перерыв ☕ вы его заслужили 🤗

```python
# НЕОБХОДИМО СДЕЛАТЬ: Обучить агента на 1 000 000 временных шагов

# НЕОБХОДИМО СДЕЛАТЬ: Задание имени файла для модели и сохранение модели в файл
model_name = ""
```

#### Решение

```python
# РЕШЕНИЕ
# Обучить агента на 1 000 000 временных шагов
model.learn(total_timesteps=1000000)
# Сохранить модель
model_name = "ppo-LunarLander-v2"
model.save(model_name)
```

## Оценка агента 📈

- Не забудьте обернуть среду в [Monitor](https://stable-baselines3.readthedocs.io/en/master/common/monitor.html).
- Теперь, когда наш агент Lunar Lander обучен 🚀, нам необходимо **проверить его качество**.
- Stable-Baselines3 предоставляет метод, позволяющий сделать это : `evaluate_policy`.
- Для заполнения этой части необходимо [посмотреть документацию](https://stable-baselines3.readthedocs.io/en/master/guide/examples.html#basic-usage-training-saving-loading)
- В следующем шаге мы рассмотрим **как автоматически оценивать и делиться своим агентом для участия в соревнованиях в таблице результатов, а пока давайте сделаем это сами**.


💡 При оценке агента следует не использовать учебную среду, а создать среду оценки.

```python
# НЕОБХОДИМО СДЕЛАТЬ: Оценить агента
# Создаем новую среду для оценки
eval_env =

# Оценить модель с 10 эпизодами оценки и deterministic=True
mean_reward, std_reward = 

# Выведем результаты
```

#### Решение

```python
# @title
eval_env = Monitor(gym.make("LunarLander-v2"))
mean_reward, std_reward = evaluate_policy(model, eval_env, n_eval_episodes=10, deterministic=True)
print(f"mean_reward={mean_reward:.2f} +/- {std_reward}")
```

- В моем случае после обучения в течение 1 млн. шагов я получил среднее вознаграждение `200.20 +/- 20.80`, что означает, что наш агент луноход готов к посадке на Луну 🌛🥳.

## Публикация обученной модели на Hub 🔥
Теперь, когда мы увидели, что получили хорошие результаты после обучения, мы можем опубликовать нашу обученную модель на хабе 🤗 с помощью одной строки кода.

📚 Документация по библиотекам 👉 https://github.com/huggingface/huggingface_sb3/tree/main#hugging-face--x-stable-baselines3-v20

Вот пример карточки модели (с игрой Space Invaders):

Используя `package_to_hub` **вы оцениваете, записываете реплей, генерируете карту модели вашего агента и загружаете его в хаб**.

Таким образом:
- Вы можете **продемонстрировать нашу работу** 🔥
- Вы можете **визуализировать игру своего агента** 👀
- Вы можете **поделиться с сообществом агентом, который может быть использован другими** 💾
- Вы можете **зайти в таблицу результатов 🏆, чтобы увидеть, насколько хорошо ваш агент работает по сравнению с вашими однокурсниками** 👉 https://huggingface.co/spaces/huggingface-projects/Deep-Reinforcement-Learning-Leaderboard


Чтобы получить возможность поделиться своей моделью с сообществом, необходимо выполнить еще три шага:

1️⃣ (Если это еще не сделано) создать учетную запись на сайте Hugging Face ➡ https://huggingface.co/join

2️⃣ Войдите в аккаунт, после чего необходимо сохранить токен аутентификации с сайта Hugging Face.
- Создайте новый токен (https://huggingface.co/settings/tokens) **с правами на запись**

<img src="https://huggingface.co/datasets/huggingface-deep-rl-course/course-images/resolve/main/en/notebooks/create-token.jpg" alt="Create HF Token">

- Скопируйте токен 
- Выполните ячейку ниже и вставьте токен

```python
notebook_login()
!git config --global credential.helper store
```

Если вы не хотите использовать Google Colab или Jupyter Notebook, то вместо этого необходимо использовать данную команду: `huggingface-cli login`

3️⃣ Теперь мы готовы передать нашего обученного агента в 🤗 хаб 🔥 с помощью функции `package_to_hub()`.

Давайте заполним функцию `package_to_hub`:
- `model`: наша обученная модель.
- `model_name`: имя обученной модели, которое мы определили в `model_save`
- `model_architecture`: используемая нами архитектура модели, в нашем случае PPO
- `env_id`: имя среды, в нашем случае `LunarLander-v2`
- `eval_env`: the evaluation environment defined in eval_env
- `repo_id`: the name of the Hugging Face Hub Repository that will be created/updated `(repo_id = {username}/{repo_name})`

💡 **Хорошее название - это `{имя пользователя}/{модель_архитектура}-{идентификатор_среды}` **

- `commit_message`: сообщение о коммите

```python
import gymnasium as gym
from stable_baselines3.common.vec_env import DummyVecEnv
from stable_baselines3.common.env_util import make_vec_env

from huggingface_sb3 import package_to_hub

## НЕОБХОДИМО СДЕЛАТЬ: Определить repo_id
## repo_id - идентификатор репозитория модели на Hugging Face Hub (repo_id = {organization}/{repo_name} например ThomasSimonini/ppo-LunarLander-v2
repo_id = 

# НЕОБХОДИМО СДЕЛАТЬ: Определите имя среды
env_id = 

# Создайте evaluation env и установите render_mode="rgb_array"
eval_env = DummyVecEnv([lambda: gym.make(env_id, render_mode="rgb_array")])


# НЕОБХОДИМО СДЕЛАТЬ: Определить архитектуру используемой модели
model_architecture = ""

## НЕОБХОДИМО СДЕЛАТЬ: Определить сообщение о commit
commit_message = ""

# метод save, evaluate, генерирует карточку модели и записывает видео с воспроизведением работы агента перед отправкой репозиторий на хаб
package_to_hub(model=model, # Наша обученная модель
               model_name=model_name, # Имя нашей обученной модели
               model_architecture=model_architecture, # Используемая нами архитектура модели: в нашем случае PPO
               env_id=env_id, # Наименование среды
               eval_env=eval_env, # EСреда оценки
               repo_id=repo_id, # id репозитория модели из Hugging Face Hub (repo_id = {организация}/{имя_репозитория} для примера ThomasSimonini/ppo-LunarLander-v2
               commit_message=commit_message)
```

#### Решение


```python
import gymnasium as gym

from stable_baselines3 import PPO
from stable_baselines3.common.vec_env import DummyVecEnv
from stable_baselines3.common.env_util import make_vec_env

from huggingface_sb3 import package_to_hub

# Разместите переменные, которые вы только что определили двумя ячейками выше
# Определите имя среды
env_id = "LunarLander-v2"

# НЕОБХОДИМО СДЕЛАТЬ: Определить архитектуру модели, которую мы использовали
model_architecture = "PPO"

## Определите repo_id
## repo_id - это идентификатор репозитория модели из Hugging Face Hub (repo_id = {организация}/{имя_репозитория}, например ThomasSimonini/ppo-LunarLander-v2
## ИЗМЕНИТЕ НА СВОЙ ИДЕНТИФИКАТОР РЕПОЗИТОРИЯ
repo_id = "ThomasSimonini/ppo-LunarLander-v2"  # Измените на свой идентификатор репозитория, с моим вы не сможете выполнить push 😄

## Определить сообщение commit
commit_message = "Upload PPO LunarLander-v2 trained agent"

# Создаем evaluation env и задаем render_mode="rgb_array"
eval_env = DummyVecEnv([lambda: Monitor(gym.make(env_id, render_mode="rgb_array"))])

# Поместите сюда только что заполненную функцию package_to_hub
package_to_hub(
    model=model,  # Наша обученная модель
    model_name=model_name,  # Имя нашей обученной модели
    model_architecture=model_architecture,  # Используемая нами архитектура модели: в нашем случае PPO
    env_id=env_id,  # Название среды
    eval_env=eval_env,  # Среда оценки
    repo_id=repo_id,  # id репозитория модели из Hugging Face Hub (repo_id = {организация}/{имя_репозитория} для примера ThomasSimonini/ppo-LunarLander-v2
    commit_message=commit_message,
)
```

Поздравляем 🥳 Вы только что обучили и загрузили своего первого агента глубокого обучения с подкреплением. Приведенный выше сценарий должен был отобразить ссылку на репозиторий модели, например https://huggingface.co/osanseviero/test_sb3. Перейдя по этой ссылке, вы сможете:
* Посмотреть видеопревью вашего агента справа.  
* Нажмите кнопку "Files and versions", чтобы увидеть все файлы в репозитории.
* Нажмите кнопку "Use in stable-baselines3", чтобы получить фрагмент кода, демонстрирующий загрузку модели.
* Карточка модели (файл `README.md`), в которой приводится описание модели

Под капотом Hub использует репозитории на основе git (не волнуйтесь, если вы не знаете, что такое git), что означает, что вы можете обновлять модель новыми версиями по мере проведения экспериментов и совершенствования своего агента.

Сравните результаты LunarLander-v2 со своими однокурсниками с помощью таблицы результатов 🏆 👉 https://huggingface.co/spaces/huggingface-projects/Deep-Reinforcement-Learning-Leaderboard

## Загрузка сохраненной модели LunarLander из Hub 🤗
Благодарим [ironbar](https://github.com/ironbar) за вклад.

LЗагрузить сохраненную модель из Hub очень просто. 

Вы приходите к https://huggingface.co/models?library=stable-baselines3 чтобы увидеть список всех сохраненных моделей Stable-baselines3.
1. Вы выбираете одну из них и копируете ее repo_id

<img src="https://huggingface.co/datasets/huggingface-deep-rl-course/course-images/resolve/main/en/notebooks/unit1/copy-id.png" alt="Copy-id"/>

2. Затем нужно просто использовать load_from_hub с:
- Идентификатором repo_id
- Имя файла: сохраненная внутри репозитория модель и ее расширение (*.zip)

Поскольку модель, которую я загружаю с хаба, была обучена с помощью Gym (бывшая версия Gymnasium), нам необходимо установить shimmy - инструмент преобразования API, который поможет нам корректно запустить среду.

Документация Shimmy: https://github.com/Farama-Foundation/Shimmy

```python
!pip install shimmy
```

```python
from huggingface_sb3 import load_from_hub

repo_id = "Classroom-workshop/assignment2-omar"  # The repo_id
filename = "ppo-LunarLander-v2.zip"  # The model filename.zip

При обучении модели на Python 3.8 значение версии протокола pickle равно 5
# Однако в Python 3.6, 3.7 используется протокол версии 4
# Для обеспечения совместимости нам необходимо:
# 1. установить pickle5 (мы сделали это в начале ноутбука)
# 2. Создать пустой пользовательский объект, который мы передадим в качестве параметра в PPO.load()
custom_objects = {
    "learning_rate": 0.0,
    "lr_schedule": lambda _: 0.0,
    "clip_range": lambda _: 0.0,
}

checkpoint = load_from_hub(repo_id, filename)
model = PPO.load(checkpoint, custom_objects=custom_objects, print_system_info=True)
```

Давайте оценим этого агента:

```python
# @title
eval_env = Monitor(gym.make("LunarLander-v2"))
mean_reward, std_reward = evaluate_policy(model, eval_env, n_eval_episodes=10, deterministic=True)
print(f"mean_reward={mean_reward:.2f} +/- {std_reward}")
```

## Некоторые дополнительные задачи 🏆
Лучший способ научиться **это попробовать все самому**! Как вы видели, текущий агент работает не очень хорошо. В качестве первого предложения можно обучать агента на большему количеству шагов. С 1 000 000 шагов мы увидели отличные результаты!  

В [таблице результатов](https://huggingface.co/spaces/huggingface-projects/Deep-Reinforcement-Learning-Leaderboard) вы найдете своих агентов. Сможете ли вы добраться до вершины?

Вот несколько идей, как этого добиться:
* Обучать на большем количестве шагов
* Попробуйте различные гиперпараметры для `PPO`. Вы можете посмотреть их в https://stable-baselines3.readthedocs.io/en/master/modules/ppo.html#parameters.
* Ознакомьтесь с [документацией Stable-Baselines3](https://stable-baselines3.readthedocs.io/en/master/modules/dqn.html)и попробуйте другую модель, например, DQN.
* **Опубликуйте новую обученную модель** в Hub 🔥

**Сравните результаты своей модели LunarLander-v2 с моделями своих однокурсников**, используя [таблицу результатов](https://huggingface.co/spaces/huggingface-projects/Deep-Reinforcement-Learning-Leaderboard) 🏆

Высадка на Луну - это слишком скучно для вас? Попробуйте **изменить среду**, почему бы не использовать MountainCar-v0, CartPole-v1 или CarRacing-v0? Проверьте, как они работают [используя документацию Gym](https://www.gymlibrary.dev/)  и получайте удовольствие 🎉.

________________________________________________________________________
Поздравляю с окончанием этой главы! Это была самая большая глава, **и в ней было много информации**.

Если вы все еще чувствуете, что запутались во всех этих элементах... это абсолютно нормально! **Так было и со мной, и со всеми, кто изучал RL.**

Потратьте время на то, чтобы действительно **усвоить материал, прежде чем продолжать и пробовать дополнительные задачи.** Важно освоить эти элементы и заложить прочный фундамент.

Естественно, в ходе курса мы будем углубляться в эти понятия, но **лучше иметь представление о них сейчас, прежде чем погружаться в следующие главы.**


В следующий раз, в бонусном блоке 1, вы будете обучать собаку Huggy приносить палку.

<img src="https://huggingface.co/datasets/huggingface-deep-rl-course/course-images/resolve/main/en/notebooks/unit1/huggy.jpg" alt="Huggy"/>

## Продолжайте учиться, оставайтесь потрясающими 🤗
